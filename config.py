"""
تنظیمات مرکزی پروژه.
تمام مقادیر ثابت (Constants) که در بخش‌های مختلف اسکریپت استفاده می‌شوند این‌جا تعریف شده‌اند
تا تغییر پارامترها بدون دست‌کاری منطق کد ممکن باشد.
"""

from pathlib import Path

# ---------------------------------------------------------------------------
# آدرس پلتفرم — حتماً حساب دمو (Demo) استفاده شود.
# ---------------------------------------------------------------------------
POCKET_OPTION_URL = "https://pocketoption.com/en/cabinet/try-demo/"

# ---------------------------------------------------------------------------
# پارامترهای بافر تیک و محاسبات سرعت/شتاب
# ---------------------------------------------------------------------------
TICK_BUFFER_SIZE = 10            # تعداد آخرین تیک‌ها برای محاسبهٔ سرعت/شتاب لحظه‌ای
TICK_HISTORY_SIZE = 200          # بافر بلندتر تیک‌ها برای Spike Z-Score و تشخیص Stall
CANDLE_TIMEFRAME_SECONDS = 60    # تایم فریم کندل (۱ دقیقه)

# کندل‌ساز دومِ ریزتر (پیش‌فرض ۵ ثانیه) - موازی با کندل‌ساز اصلی از همان جریان
# تیک تغذیه می‌شود تا مدل علاوه بر شکل کندل‌های یک‌دقیقه‌ای، شکل نرمال‌شدهٔ
# چند کندل ریزِ چندثانیه‌ای منتهی به لحظهٔ ورود به معامله را هم ببیند - دقیقاً
# مفید برای معاملاتی با انقضای کوتاه (چند ثانیه‌ای) که رفتار درون همان چند
# ثانیهٔ آخر اهمیت زیادی دارد.
MICRO_CANDLE_TIMEFRAME_SECONDS = 5
MICRO_CHART_WINDOW_CANDLES = 10  # تعداد کندل ریزِ اخیر در «شکل چارت» ریز (همان قاعدهٔ CHART_WINDOW_CANDLES)

# ---------------------------------------------------------------------------
# پارامترهای ساختار بازار (Swing / Leg / Support-Resistance / Chart Shape)
# ---------------------------------------------------------------------------
CHART_WINDOW_CANDLES = 10        # تعداد کندل اخیر برای «شکل چارت» نرمال‌شده (برابر با پنجرهٔ دیداری واقعی شما)
CHART_WINDOW_CANDLES_LONG = 25   # پنجرهٔ دوم و بزرگ‌تر «شکل چارت» - برای الگوهای بزرگ‌تر/بافت کلی‌تر بازار
# باید به‌اندازهٔ کافی بزرگ باشد تا هم بزرگ‌ترین پنجرهٔ چارت‌شیپ و هم چند سوئینگ قبل از آن‌ها را پوشش دهد
CANDLE_HISTORY_SIZE = 60
SWING_FRACTAL_K = 1              # تعداد کندل همسایه برای تشخیص سوئینگ (فرکتال)
SR_LOOKBACK_SWINGS = 5           # تعداد آخرین سوئینگ‌ها برای تعیین مقاومت/حمایت «اخیر»
ATR_PERIOD_SHORT = 5             # دورهٔ ATR کوتاه (برای انبساط/انقباض نوسان)
ATR_PERIOD_LONG = 20             # دورهٔ ATR بلند (مرجع نرمال‌سازی فاصله تا حمایت/مقاومت)
LEG_RATIO_LOOKBACK = 3           # تعداد لگ قبلی برای محاسبهٔ نسبت طول لگ فعلی
WICK_ASYMMETRY_LOOKBACK = 10     # تعداد کندل اخیر برای میانگین عدم‌تقارن سایه‌ها
TREND_SWING_LOOKBACK = 2         # تعداد سوئینگ آخر برای تشخیص رژیم روند (صعودی/نزولی/رنج)

# ---------------------------------------------------------------------------
# پارامترهای معامله
# ---------------------------------------------------------------------------
TRADE_EXPIRY_SECONDS = 3         # مدت انقضای معامله (۳ ثانیه طبق روش شما)
RECENT_RESULTS_WINDOW = 3        # تعداد نتایج اخیر برای الگوی Win/Loss (مثل [1,0,1])
WINRATE_ROLLING_WINDOW = 20      # پنجره محاسبه وین‌ریت غلتان (Rolling Winrate)

# تعداد معاملهٔ پیشنهادی برای یک نمونهٔ آموزشی اولیه (فقط برای نمایش پیشرفت در
# ترمینال؛ روی هیچ منطق دیگری اثر نمی‌گذارد). صفر یا None کنید تا نمایش پیشرفت خاموش شود.
MIN_TRADES_FOR_INITIAL_TRAINING = 500

# ---------------------------------------------------------------------------
# تشخیص کلیک واقعی روی دکمه‌های BUY/SELL خودِ پلتفرم
# ---------------------------------------------------------------------------
# متن دکمه‌ها همان‌طور که در رابط Pocket Option دیده می‌شود (بزرگ/کوچک بودن حروف مهم نیست).
# اگر پلتفرم این متن‌ها را تغییر داد یا به زبان دیگری نمایش داد، این دو مقدار را اصلاح کنید.
BUY_BUTTON_TEXT = "BUY"
SELL_BUTTON_TEXT = "SELL"

# بعد از رسیدن به زمان انقضای معامله، تا چند ثانیهٔ اضافه منتظر پیام «نتیجهٔ معامله»
# از WebSocket خودِ پلتفرم بمانیم؟ اگر در این بازه چیزی پیدا نشد، به محاسبهٔ داخلی
# (بر اساس تیک‌های خودمان) برگشت می‌کنیم.
DEAL_RESULT_WAIT_SECONDS = 5

# اگر قیمت خروج دقیقاً برابر قیمت ورود باشد (Doji/تساوی)، آیا معامله باخت حساب شود؟
# اکثر بروکرها در این حالت معامله را Loss یا Refund در نظر می‌گیرند؛ این مقدار قابل تنظیم است.
# توجه: این فقط برای مسیر محاسبهٔ داخلی (tick_fallback، وقتی نتیجهٔ خودِ پلتفرم
# پیدا/تفسیر نشود) استفاده می‌شود - برای ریفاند واقعیِ اعلام‌شده توسط خودِ
# پلتفرم به REFUND_COUNTS_AS_LOSS زیر نگاه کنید.
TIE_COUNTS_AS_LOSS = True

# اگر خودِ پلتفرم صراحتاً معامله را ریفاند کند (پیام نتیجهٔ معامله از WebSocket
# فیلد refundTime/refundTimestamp غیر-null داشته باشد - معمولاً به‌خاطر مشکل
# سرور یا تساوی قیمت، نه نتیجهٔ واقعیِ معامله)، آیا این باخت حساب شود؟ چون
# ریفاند یعنی اصل مبلغ برگشته (نه سود واقعی نه ضرر واقعی)، پیش‌فرض False است
# تا نه دیتاست ترین با لیبل نادرست «باخت» آلوده شود، نه شمارندهٔ باخت متوالی
# (CONSECUTIVE_LOSSES_FOR_COOLDOWN) با یک ریفاند بی‌ربط افزایش پیدا کند.
REFUND_COUNTS_AS_LOSS = False

# ---------------------------------------------------------------------------
# نظارت بر پی‌آوت واقعی (بر اساس نتیجهٔ برگشتی خودِ پلتفرم، نه نمایش صفحه)
# ---------------------------------------------------------------------------
# چون نمایش پی‌آوت روی صفحه ممکن است با پی‌آوت واقعاً اعمال‌شده فرق داشته باشد،
# پی‌آوت هر معامله از روی نتیجهٔ برگشتی خودِ پلتفرم (سود/مبلغ در پیام WebSocket
# نتیجهٔ معامله) محاسبه می‌شود، نه از روی متن صفحه. اگر این عدد کمتر از این
# آستانه بیفتد، معاملهٔ برنامه‌ای (level_strategy / AUTO_TRADE) متوقف می‌شود و
# یک بنر هشدار روی خودِ صفحهٔ مرورگر نمایش داده می‌شود. معاملهٔ دستی شما هرگز
# مسدود نمی‌شود - این فقط جلوی کلیک خودکار اسکریپت‌ها را می‌گیرد.
MIN_PAYOUT_PERCENT = 92

# چون فرمت پیام «نتیجهٔ معامله» مستند نیست، تشخیص آن یک حدس هیوریستیک است و
# گاهی ممکن است اشتباهی رکورد یک معاملهٔ دیگر/قدیمی (نه همین معاملهٔ تازه‌بسته‌
# شده) را به‌جای نتیجهٔ واقعی بگیرد - که یک عدد پی‌آوت کاذب و گذرا می‌سازد. برای
# جلوگیری از توقف اشتباهی به‌خاطر یک خوانش نادرست تکی، توقف واقعی فقط زمانی
# اعمال می‌شود که این تعداد معاملهٔ متوالی (نه لزوماً پشت‌سرهم زمانی، بلکه در
# میان معاملاتی که پی‌آوتشان اصلاً قابل تشخیص بود) همگی زیر آستانه باشند.
LOW_PAYOUT_CONFIRM_TRADES = 2

# ---------------------------------------------------------------------------
# توقف کوتاه‌مدت بعد از باخت‌های متوالی
# ---------------------------------------------------------------------------
# اگر معاملات برنامه‌ای (خودکار، نه دستی) به همین تعداد پشت‌سرهم باخت باشند،
# معاملهٔ خودکار برای مدتی متوقف می‌شود. برخلاف توقف پی‌آوت پایین (که نیاز به
# دستور «resume» دستی دارد)، این توقف کوتاه‌مدت است و بعد از گذشت
# CONSECUTIVE_LOSS_COOLDOWN_SECONDS ثانیه خودش به‌طور خودکار تمام می‌شود.
# معاملهٔ دستی شما هرگز توسط این مکانیزم مسدود نمی‌شود.
CONSECUTIVE_LOSSES_FOR_COOLDOWN = 2

# مدت این توقف کوتاه (بر حسب ثانیه) بعد از رسیدن به تعداد باخت متوالی بالا.
CONSECUTIVE_LOSS_COOLDOWN_SECONDS = 15

# ---------------------------------------------------------------------------
# مسیر ذخیره‌سازی داده‌ها
# ---------------------------------------------------------------------------
DATA_DIR = Path(__file__).parent / "data"
DATA_DIR.mkdir(exist_ok=True)

CSV_LOG_PATH = DATA_DIR / "trades_dataset.csv"
SQLITE_DB_PATH = DATA_DIR / "trades_dataset.sqlite3"
RAW_WS_LOG_PATH = DATA_DIR / "raw_ws_frames.log"   # برای دیباگ و شناسایی فرمت واقعی پیام‌های وب‌ساکت

# ---------------------------------------------------------------------------
# مسیر خروجی اسکریپت تشخیصی (capture_diagnostics.py)
# ---------------------------------------------------------------------------
DIAGNOSTICS_DIR = DATA_DIR / "diagnostics"

# آیا فریم‌های خام وب‌ساکت هم برای بررسی دستی ذخیره شوند؟ (توصیه می‌شود بار اول True باشد)
DEBUG_LOG_RAW_FRAMES = True

# مهلت اولیه (ثانیه) برای برقراری اولین اتصال WebSocket درست بعد از لاگین،
# قبل از این‌که Watchdog اصلاً شروع به نظارت/Reload کند (این تأخیر طبیعی است).
INITIAL_CONNECTION_GRACE_SECONDS = 30

# ---------------------------------------------------------------------------
# معاملهٔ خودکار زنده با مدل آموزش‌دیده (Live Auto-Trading)
# ---------------------------------------------------------------------------
# هشدار مهم: اگر True باشد، ربات خودش و بدون تأیید شما روی دکمهٔ BUY/SELL
# واقعی پلتفرم کلیک می‌کند. فقط روی حساب دمو فعال کنید و حتماً چند دقیقهٔ
# اول را با چشم زیر نظر بگیرید.
AUTO_TRADE_ENABLED = False

MODEL_DIR = DATA_DIR / "models"
MODEL_DIR.mkdir(exist_ok=True)
MODEL_JSON_PATH = MODEL_DIR / "pocket_option_xgb_model.json"
MODEL_FEATURES_PATH = MODEL_DIR / "feature_names.json"

# مدل مقایسه‌ای که فقط با N فیچر مهم‌تر (طبق train/train_model.py) آموزش
# می‌بیند - برای مقایسه و درک نقش بقیهٔ فیچرها ساخته شد، اما طبق تست‌ها دقتش
# عملاً هم‌تراز مدل کامل است (با فیچرهای خیلی کمتر - سبک‌تر و سریع‌تر).
MODEL_TOP_FEATURES_JSON_PATH = MODEL_DIR / "pocket_option_xgb_model_top_features.json"
MODEL_TOP_FEATURES_LIST_PATH = MODEL_DIR / "feature_names_top_features.json"

# این‌که معاملهٔ زندهٔ main.py از کدام مدل استفاده کند:
#   "full"          -> مدل کامل با همهٔ فیچرها (MODEL_JSON_PATH / MODEL_FEATURES_PATH)
#   "top_features"  -> مدل سبک‌تر با فقط فیچرهای مهم + جهت‌دار اجباری
#                      (MODEL_TOP_FEATURES_JSON_PATH / MODEL_TOP_FEATURES_LIST_PATH)
LIVE_MODEL_VARIANT = "top_features"

if LIVE_MODEL_VARIANT == "top_features":
    LIVE_MODEL_JSON_PATH = MODEL_TOP_FEATURES_JSON_PATH
    LIVE_MODEL_FEATURES_PATH = MODEL_TOP_FEATURES_LIST_PATH
else:
    LIVE_MODEL_JSON_PATH = MODEL_JSON_PATH
    LIVE_MODEL_FEATURES_PATH = MODEL_FEATURES_PATH

# حداقل احتمال بردی که مدل باید برای یک جهت پیش‌بینی کند تا معامله واقعاً
# باز شود (بین ۰ و ۱). هرچه بالاتر، معاملات کمتر ولی مطمئن‌تر.
AUTO_TRADE_CONFIDENCE_THRESHOLD = 0.70

# حداقل فاصلهٔ زمانی (ثانیه) بین دو معاملهٔ خودکار متوالی - برای این‌که ربات
# قبل از دیدن نتیجهٔ معاملهٔ قبلی، بلافاصله معاملهٔ جدید باز نکند.
AUTO_TRADE_COOLDOWN_SECONDS = 5

# حداقل اختلاف بین احتمال برد CALL و PUT تا اصلاً یک جهت «برنده» در نظر گرفته
# شود. اگر مدل عملاً بین دو جهت تفاوتی قائل نباشد (اختلاف کمتر از این مقدار)،
# یعنی سیگنال جهت‌داری در این لحظه ندارد و آن لحظه رد (Skip) می‌شود - به‌جای
# این‌که به‌طور پیش‌فرض یکی از دو جهت انتخاب شود.
AUTO_TRADE_MIN_DIRECTION_MARGIN = 0.01

# ---------------------------------------------------------------------------
# ربات مخصوص جمع‌آوری داده با استراتژی ثابت تست حمایت/مقاومت (collect_data.py)
# ---------------------------------------------------------------------------
# این استراتژی برخلاف AUTO_TRADE (که بر اساس مدل و آستانهٔ اطمینان تصمیم
# می‌گیرد)، یک قانون ثابت است و صرفاً برای جمع‌آوری سریع و متنوع داده استفاده
# می‌شود - وین‌ریت این معاملات مهم نیست.

# حداقل فاصلهٔ زمانی (ثانیه) بین دو کلیک متوالی این ربات - فقط برای جلوگیری
# از دو کلیک هم‌زمان روی صفحه، نه یک آستانهٔ کیفیت.
LEVEL_STRATEGY_MIN_CLICK_INTERVAL_SECONDS = 2

# اگر فاصلهٔ دو اکسترمم هم‌نوع (هر دو قله یا هر دو دره) داخل کندل جاری کمتر
# از این نسبت از رنج کندل باشد، «همان سطح» در نظر گرفته می‌شوند (یعنی قیمت
# دوباره تقریباً همان‌جا متوقف/برگشته است).
LEVEL_STRATEGY_MATCH_TOLERANCE_RATIO = 0.05

# کدام نسخهٔ استراتژی در collect_data.py اجرا شود:
#   "basic"     -> src/level_strategy.py (بدون تغییر نسبت به قبل)
#   "optimized" -> src/level_strategy_optimized.py (همان استراتژی + معکوس‌سازی
#                  جهت بعد از چند باخت متوالی در حالت نوسان دوطرفه - نگاه کنید
#                  به داکیومنت همان فایل)
LEVEL_STRATEGY_VARIANT = "basic"

# فقط در نسخهٔ optimized: بعد از این تعداد باخت متوالی (فقط از سیگنال‌های
# حالت نوسان دوطرفه، نه حالت‌های ۱/۲) در همان کندل، جهتِ باقیِ معاملات آن
# حالت در همان کندل معکوس می‌شود (تا کندل تمام شود).
LEVEL_STRATEGY_REVERSE_AFTER_LOSSES = 2

# ---------------------------------------------------------------------------
# مرورگر
# ---------------------------------------------------------------------------
BROWSER_HEADLESS = False         # باید False باشد تا کاربر بتواند دستی لاگین کند
USER_DATA_DIR = Path(__file__).parent / ".browser_profile"  # برای حفظ نشست لاگین بین اجراها

# بعضی پلتفرم‌ها Chromium باندل‌شدهٔ Playwright را «مرورگر ناشناخته» تشخیص
# می‌دهند و لاگین را رد می‌کنند، درحالی‌که با نصب واقعی Google Chrome مشکلی
# ندارند. مقادیر ممکن: "chrome" (از Chrome واقعیِ نصب‌شده روی سیستم استفاده
# کن)، یا None (همان Chromium داخلی Playwright، رفتار قبلی).
# نکته: با اجرای setup_chrome_profile.py می‌توانید یک نسخهٔ اولیهٔ
# USER_DATA_DIR را از پروفایل واقعیِ Chrome روزمرهٔ خودتان (که از قبل روی آن
# لاگین کرده‌اید) بسازید - این‌طوری اصلاً نیازی به تکرار فرایند لاگین
# نمی‌ماند و همان نشستِ از قبل تأییدشده استفاده می‌شود.
BROWSER_CHANNEL = "chrome"
