"""
تنظیمات مرکزی پروژه.
تمام مقادیر ثابت (Constants) که در بخش‌های مختلف اسکریپت استفاده می‌شوند این‌جا تعریف شده‌اند
تا تغییر پارامترها بدون دست‌کاری منطق کد ممکن باشد.
"""

from pathlib import Path

# ---------------------------------------------------------------------------
# آدرس پلتفرم — حتماً حساب دمو (Demo) استفاده شود.
# ---------------------------------------------------------------------------
POCKET_OPTION_URL = "https://pocketoption.com/en/cabinet/try-demo/"

# ---------------------------------------------------------------------------
# پارامترهای بافر تیک و محاسبات سرعت/شتاب
# ---------------------------------------------------------------------------
TICK_BUFFER_SIZE = 10            # تعداد آخرین تیک‌ها برای محاسبهٔ سرعت/شتاب لحظه‌ای
TICK_HISTORY_SIZE = 200          # بافر بلندتر تیک‌ها برای Spike Z-Score و تشخیص Stall
CANDLE_TIMEFRAME_SECONDS = 60    # تایم فریم کندل (۱ دقیقه)

# ---------------------------------------------------------------------------
# پارامترهای ساختار بازار (Swing / Leg / Support-Resistance / Chart Shape)
# ---------------------------------------------------------------------------
CHART_WINDOW_CANDLES = 10        # تعداد کندل اخیر برای «شکل چارت» نرمال‌شده (برابر با پنجرهٔ دیداری واقعی شما)
# باید به‌اندازهٔ کافی بزرگ باشد تا هم CHART_WINDOW_CANDLES و هم چند سوئینگ قبل از آن‌ها را پوشش دهد
CANDLE_HISTORY_SIZE = 60
SWING_FRACTAL_K = 1              # تعداد کندل همسایه برای تشخیص سوئینگ (فرکتال)
SR_LOOKBACK_SWINGS = 5           # تعداد آخرین سوئینگ‌ها برای تعیین مقاومت/حمایت «اخیر»
ATR_PERIOD_SHORT = 5             # دورهٔ ATR کوتاه (برای انبساط/انقباض نوسان)
ATR_PERIOD_LONG = 20             # دورهٔ ATR بلند (مرجع نرمال‌سازی فاصله تا حمایت/مقاومت)
LEG_RATIO_LOOKBACK = 3           # تعداد لگ قبلی برای محاسبهٔ نسبت طول لگ فعلی
WICK_ASYMMETRY_LOOKBACK = 10     # تعداد کندل اخیر برای میانگین عدم‌تقارن سایه‌ها
TREND_SWING_LOOKBACK = 2         # تعداد سوئینگ آخر برای تشخیص رژیم روند (صعودی/نزولی/رنج)

# ---------------------------------------------------------------------------
# پارامترهای معامله
# ---------------------------------------------------------------------------
TRADE_EXPIRY_SECONDS = 3         # مدت انقضای معامله (۳ ثانیه طبق روش شما)
RECENT_RESULTS_WINDOW = 3        # تعداد نتایج اخیر برای الگوی Win/Loss (مثل [1,0,1])
WINRATE_ROLLING_WINDOW = 20      # پنجره محاسبه وین‌ریت غلتان (Rolling Winrate)

# ---------------------------------------------------------------------------
# تشخیص کلیک واقعی روی دکمه‌های BUY/SELL خودِ پلتفرم
# ---------------------------------------------------------------------------
# متن دکمه‌ها همان‌طور که در رابط Pocket Option دیده می‌شود (بزرگ/کوچک بودن حروف مهم نیست).
# اگر پلتفرم این متن‌ها را تغییر داد یا به زبان دیگری نمایش داد، این دو مقدار را اصلاح کنید.
BUY_BUTTON_TEXT = "BUY"
SELL_BUTTON_TEXT = "SELL"

# بعد از رسیدن به زمان انقضای معامله، تا چند ثانیهٔ اضافه منتظر پیام «نتیجهٔ معامله»
# از WebSocket خودِ پلتفرم بمانیم؟ اگر در این بازه چیزی پیدا نشد، به محاسبهٔ داخلی
# (بر اساس تیک‌های خودمان) برگشت می‌کنیم.
DEAL_RESULT_WAIT_SECONDS = 5

# اگر قیمت خروج دقیقاً برابر قیمت ورود باشد (Doji/تساوی)، آیا معامله باخت حساب شود؟
# اکثر بروکرها در این حالت معامله را Loss یا Refund در نظر می‌گیرند؛ این مقدار قابل تنظیم است.
TIE_COUNTS_AS_LOSS = True

# ---------------------------------------------------------------------------
# مسیر ذخیره‌سازی داده‌ها
# ---------------------------------------------------------------------------
DATA_DIR = Path(__file__).parent / "data"
DATA_DIR.mkdir(exist_ok=True)

CSV_LOG_PATH = DATA_DIR / "trades_dataset.csv"
SQLITE_DB_PATH = DATA_DIR / "trades_dataset.sqlite3"
RAW_WS_LOG_PATH = DATA_DIR / "raw_ws_frames.log"   # برای دیباگ و شناسایی فرمت واقعی پیام‌های وب‌ساکت

# ---------------------------------------------------------------------------
# مسیر خروجی اسکریپت تشخیصی (capture_diagnostics.py)
# ---------------------------------------------------------------------------
DIAGNOSTICS_DIR = DATA_DIR / "diagnostics"

# آیا فریم‌های خام وب‌ساکت هم برای بررسی دستی ذخیره شوند؟ (توصیه می‌شود بار اول True باشد)
DEBUG_LOG_RAW_FRAMES = True

# ---------------------------------------------------------------------------
# مرورگر
# ---------------------------------------------------------------------------
BROWSER_HEADLESS = False         # باید False باشد تا کاربر بتواند دستی لاگین کند
USER_DATA_DIR = Path(__file__).parent / ".browser_profile"  # برای حفظ نشست لاگین بین اجراها
